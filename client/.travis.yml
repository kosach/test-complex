sudo: required
services:
  - docker
before_install:
  - docker build -t kosach/react-test -f ./client/Dockerfile.dev ./client
script:
  - docker run kosach/react-test npm test -- --coverage
after_success:
  - docker build -t kosach/multi-client ./client
  - docker build -t kosach/multi-nginx ./nginx
  - docker build -t kosach/multi-server ./server
  - docker build -t kosach/multi-worker ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push kosach/multi-client
  - docker push kosach/multi-nginx
  - docker push kosach/multi-server
  - docker push kosach/multi-worker